// function min(a, b):
//     if a < b:
//         return a
//      else:
//         return b

//  print(min(3, 4)) # prints 3
//  print(min(5, 2)) # prints 2

// compare firstNum and secondNum and return the 
//smaller of the two

// function min( firstNum, secondNum ) {
// 	if ( firstNum < secondNum )
//       return firstNum;
//     else
//       return secondNum;
// }

// console.log(min(firstNum, secondNum));

function min(a, b) {
    if (a < b) {
      return a;
    } else {
      return b;
    }
  }
  
  console.log(min(30, 16));
